# AIM
The goal of this project was to practice NodeJS. Here, the launch command will create random users in a json file, by using the CLI and specifying yes or no.

# Project launch

` npm run start `